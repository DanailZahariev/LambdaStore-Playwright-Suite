{
  "name": "lambdastore",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "generate:data": "ts-node utils/test-data/generate-script.ts",
    "test": "npm run generate:data && playwright test",
    "test:ui": "npm run generate:data && playwright test --ui",
    "build": "tsc"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.2.0",
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  },
  "private": true
}
